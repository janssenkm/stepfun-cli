/** Package version embedded in the compiled CLI. */
export const CLI_VERSION = '0.1.1';

/** Stable client identifier attached to every model API request. */
export const USER_AGENT = `stepfun-cli/${CLI_VERSION}`;
